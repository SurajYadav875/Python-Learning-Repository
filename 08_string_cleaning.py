#Whitespace cleanup
is_clean=len(Course1)==len(Course1.strip())
Topics
strip()
lstrip()
rstrip()
Removing special characters
Checking for whitespace
Data cleaning
Notes
text.strip()

removes leading and trailing whitespace.

text.lstrip()

removes whitespace from the left.

text.rstrip()

removes whitespace from the right.

This is very important for Data Analyst work because real-world datasets often contain unwanted spaces.

  
